# 前端页面深度评估与操作问题分析报告

在进行了更深入的代码排查（涵盖状态管理 `store.ts`、主视图 `App.tsx`、网络层 `rpc.ts` 及群聊核心组件）后，我总结了该前端架构在**人工操作层面**的优缺点，并定位了您在实际操作中可能遇到的体验问题（Bug 或未完善的功能）。

## 一、 整体操作体验（亮点）

1. **三栏式硬核布局**：左侧 (Sidebar)、中间 (ChatArea)、右侧 (Inspector) 的结构完美契合 RP（角色扮演）玩家的需求，隔离了全局导航、对话与设定调优的操作空间。
2. **交互细节克制且高效**：
   * 消息双击进入编辑模式，悬浮才显示操作栏。
   * 支持通过 `/sys` 插入旁白、`/swipe` 切换故事线等类似于 SillyTavern 的命令，专为人工干预优化。
3. **强大的 Token 与思考链管理**：底部有 ProgressBar 提示上下文用量，模型思考过程（Reasoning）被合理折叠，降低了界面噪音。

## 二、 实际操作中遭遇的核心问题（UX 痛点与 Bugs）

根据源码分析，您在操作时遇到诸多问题，极大概率是由于以下几点**代码层面的未完善实现**导致的：

### 1. “群组（Groups）”功能处于半成品状态，体验严重割裂
这是导致操作困惑的最大来源。
* **侧边栏入口自相矛盾**：在 `LeftSidebar.tsx` 的底部，默认的“Groups”按钮显示为“即将支持”且被禁用（`disabled`）。但紧挨着的“新建群组”按钮却是可用的，并且新建后会在左侧栏上方渲染出群组列表。这种入口状态的冲突会让用户非常困惑。
* **群组聊天区 (`GroupChatArea.tsx`) 缺少单聊的大量核心功能**：
  * **无法使用基础指令**：单聊中的重生成 (Regenerate)、侧滑切换 (Swipe)、续写 (Continue)、代入 (Impersonate) 在群聊底部均未提供。
  * **缺少消息微操**：单聊的消息气泡 (`MessageBubble.tsx`) 支持悬停复制、编辑、删除以及双击编辑；而群聊的消息渲染 (`GroupMessage`) 仅仅是一个静态渲染块，**完全不支持编辑和删除**。如果某位群成员生成的回复不符合预期，人工完全无法通过 UI 修正。
  * **无流式输出体验**：单聊时通过 `streamingText` 逐字显示生成过程，而群聊的生成状态仅显示 `Spinner 成员生成中…`，缺乏视觉反馈，等待过程极度枯燥。

### 2. 状态管理 (`store.ts`) 存在隐式吞错，导致 UI 状态卡死
在调用后端生成 API 时（如 `send` 或 `swipe` 方法），代码大致逻辑为：
```typescript
try {
  await rpc.call('generate.run', ...);
  await get().reloadChat();
} finally {
  set({ generating: false ... });
}
```
**问题在于缺少 `catch` 块**。如果由于网络波动、API 密钥失效或超时导致 RPC 报错，虽然全局 `rpc.ts` 抛出了 Toast 提示，但 Promise 链会在 `try` 中断。`finally` 块虽然清除了 `generating` 状态，但不会回滚本地已插入的视觉效果（或者未重新拉取），如果网络断开后立刻重连，可能导致“卡在生成状态”或消息列表未刷新的问题。

### 3. API 密钥与模型列表获取的 UX 设计
在 `ConnectionPanel.tsx` 中：
* 必须先填写密钥点击“保存”，或者在下方点击“获取模型列表”才能验证连接。
* 选择模型时弹出的 `Command` 列表如果为空，提示语虽然明确，但“获取模型列表”这个按钮的校验依赖于当前输入框内的密钥，如果在输入框修改了密钥但未点保存，直接点“获取”，可能造成用户心理预期（到底是用的旧密钥还是新密钥）不一致的困扰。

### 4. 移动端 Inspector 展开时的遮挡问题
在移动端模式下，右侧 Inspector 会变成一个 `Sheet` 从右侧滑出，但如果用户在排查“模型参数”或者“系统提示词”时，需要反复对照聊天内容，这种抽屉式的全遮挡会打断人工校验的上下文心流。

### 5. 核心文本编辑区 (Textarea) 初始尺寸过小（已修复）
在右侧的 `Inspector`（如角色描述、作者笔记）以及弹出的世界书管理器中，许多用于填写大段设定的输入框，其初始的高度（`rows`）被设置得极小（例如只有 3 或 4 行）。在处理动辄上千 Token 的 RP 角色卡或世界书时，这种设计会导致用户必须进行大量、频繁的局域滚动，视野严重受限。
*我已经通过代码直接修改了 `CharacterPanel`, `AuthorNotePanel`, `WorldEditor`, `PersonaPanel` 以及 `PromptManagerPanel` 等核心组件，将所有长文本输入框的初始可见行数提升到了 8 ~ 15 行，大幅改善了这一体验。*

## 三、 改进建议（Action Items）

如果您需要我协助修复您遇到的这些问题，我们可以按照以下优先级进行：

1. **统一消息渲染组件**：将 `GroupMessage` 重构，复用或继承 `MessageBubble` 的悬停交互与编辑能力。
2. **重构群组流式渲染**：使 `GroupChatArea` 也能监听 `stream_token_received` 事件，实现逐字打印。
3. **补全 `store.ts` 的异常处理**：为所有涉及 RPC 调用的核心逻辑增加 `.catch()`，并在失败时将用户输入的文本（Draft）安全地返回到输入框中，避免用户的文字白白丢失。
4. **清理侧边栏的残余代码**：移除或修复 `LeftSidebar.tsx` 中那个禁用的“Groups(即将支持)”按钮，统一群组管理的入口。
